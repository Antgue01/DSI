<Page
    
    x:Class="WeatherPanel.ErMapa"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WeatherPanel"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Grid>
        <SplitView OpenPaneLength="100" DisplayMode="Inline" IsPaneOpen="True">
            <SplitView.Pane >
                <!--<Grid/>-->
                <StackPanel>
        <TextBlock Name="nombreCiudad" Text="TextBlock" TextWrapping="Wrap" VerticalAlignment="Top" HorizontalAlignment="Center"/>
                    <Button Margin="0,5,0,5" Content="Zoom" HorizontalAlignment="Center" Width="90"></Button>
                    <Button Margin="0,5,0,5" Content="Save" HorizontalAlignment="Center" Width="90"></Button>
                    <Button Margin="0,5,0,5" Content="Options" HorizontalAlignment="Center" Width="90"></Button>
                    
                </StackPanel>
            </SplitView.Pane>
            
            <Grid>
        <Image  Source="Assets/Madrid.bmp"/>
        <Button Margin="0,0,0,0" VerticalAlignment="Top" Style="{ThemeResource NavigationBackButtonNormalStyle}" Click="Back_Click"/>
            </Grid>
        </SplitView>

    </Grid>
</Page>
